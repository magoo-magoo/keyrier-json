# must be unique in a given SonarQube instance
sonar.projectKey=keyrier-json
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=Keyrier JSON
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
sonar.sources=ui/src,core/src,cli/src
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.exclusions=**/node_modules/**,**/*.test.tsx,src/data/**.tsx,**/*.test.ts
sonar.tests=ui/src,core/src,cli/src
sonar.test.inclusions=**/*.test.tsx
sonar.javascript.lcov.reportPaths=cli/coverage/lcov.info,core/coverage/lcov.info,ui/coverage/lcov.info